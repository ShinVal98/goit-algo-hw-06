У цьому завданні реалізовано алгоритми пошуку в глибину (DFS) та пошуку в ширину (BFS) для графа, який було створено в першому завданні.
Основна мета — порівняти результати обох алгоритмів пошуку шляху від однієї вершини до іншої.

Що зроблено:

Створено орієнтований граф міст ("City1" — "City6").
Додано різні ребра між вершинами.
Реалізовано пошук шляху між "City1" та "City6" за допомогою DFS та BFS.
Результат обох алгоритмів порівняно з поясненням різниці.

Різниця в шляхах:

DFS завжди іде максимально глибоко, поки не знайде ціль, тому шлях може бути неоптимальним.
BFS пошуком у ширину завжди дає найкоротший шлях (при рівнозважених ребрах), тому він довше за часом, але більш ефективний для пошуку мінімальної відстані.

Висновок

BFS дає кращі результати при пошуку шляху, якщо потрібно знайти саме мінімальну кількість кроків. DFS ж допоможе при пошуку глибоких шляхів в графі.
